export const environment = { 
  production: false,
  // API URLs
  apiUrl: 'http://localhost:5000',
  apiAuth: 'auth', // Auth endpoints without leading slash
  apiVersion: 'v1',
  
  // Other configurations
  tokenKey: 'auth', // Key used for storing auth in localStorage
};